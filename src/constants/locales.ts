export const LOCALES = ["ar", "en"] as const;

export const DEFAULT_LOCALE = "en";

export const LANGUAGE_LABELS = {
  ar: { label: "العربية", code: "ع" },
  en: { label: "English", code: "en" }
} as const;